---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex gap-4 text-slate-900 dark:text-slate-300">
  {
    tags.map((item) => (
      <a
        class="hover:text-orange-500 dark:hover:text-purple-500"
        href={`/tags/${item}`}
      >
        {item}
      </a>
    ))
  }
</div>
